const router = require('express').Router();

//тут будут подключены контроллеры для фильмов

router.get('/movies', _methodname);
router.post('/movies', _methodname);
router.delete('/movies/:movieId', _methodname);
